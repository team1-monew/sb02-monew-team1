spring:
  config:
    activate:
      on-profile: local

logging:
  level:
    org.springframework.web: DEBUG
    org.hibernate.SQL: DEBUG
    org.hibernate.type.descriptor.sql: TRACE
    com.team1.monew.*: DEBUG